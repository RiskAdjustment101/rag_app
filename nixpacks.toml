# Railway Nixpacks configuration for Python
[phases.setup]
nixPkgs = ["python310", "pip"]
nixLibs = []

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT --workers 1"