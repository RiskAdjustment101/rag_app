# =================================================================
# ⚠️  FRONTEND ENVIRONMENT VARIABLES - SAFE FOR BROWSER EXPOSURE
# =================================================================
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# Only put PUBLIC values here - NEVER put sensitive API keys

# Supabase Public Configuration (SAFE to expose)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_ANON_KEY

# Backend API URL (Your Railway backend)
NEXT_PUBLIC_API_URL=https://your-backend.railway.app

# Application Settings
NEXT_PUBLIC_APP_NAME="RAG App"
NEXT_PUBLIC_APP_VERSION="1.0.0"

# Feature Flags (Optional)
NEXT_PUBLIC_ENABLE_ANALYTICS=true
NEXT_PUBLIC_ENABLE_SOCIAL_LOGIN=true

# =================================================================
# 🔒 SERVER-ONLY VARIABLES (NOT exposed to browser)
# =================================================================
# These are only available in API routes and server-side code

# For server-side API calls if needed
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_SERVICE_KEY

# Analytics (Optional)
GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID
VERCEL_ANALYTICS_ID=your_vercel_analytics_id